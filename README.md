`git submodule update --init --recursive` (but for now just clone from github)
`python -m venv <venv_name>`
`source activate <venv_name>/bin/activate`
`pip install -r requirements.txt`
`<venv_name>/bin/scons`